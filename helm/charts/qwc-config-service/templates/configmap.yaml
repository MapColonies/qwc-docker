{{- if and .Values.enabled .Values.configMap.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "qwc-service.fullname" . }}-nginx
data:
  {{- toYaml .Values.configMap.data | nindent 2 }}
{{- end }}
